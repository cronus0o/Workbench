[Session 04]
Compiler (C/C++/Java) vs Interpreter (Python, Javascript)
; Comment (#, """ """, ''' ''') 

type() (int, float, str, type)
\n, \t, \r, \0, \\, \', \" 

Variable Name Regulation 
Use Alphabet and Digit
case-sensitive
alphabet first
_, not empty

Python Keywords
and as assert break class continue def del elif else export finally
for from global if import in is lambda nonlocal not or pass
raise return try while with yield

Boolean Type (True, False)

print("&d &d" & (100, 200))
%d %x %o %f %c %s
print("".format())
\n, \t, \b, \\, \', \" 

input()
int()
PEMDAS (Parentheses, Exponentiation, Multiplication, Division, Addition, Subtraction)

Arithmetic Operator : =, +, -, *, /, //, %, **
Assignment Operator : +=, -=, *=, /=, //=, %=, **=
Comparison Operator : ==, !=, >, <, >=, <=
Logical Operator : and, or, not, xor, nand, nor 
Bit Operator : &, |, ^(xor), ~, <<(*2), >>(/2)

if
if ~ else
if ~ elif ~ else
try ~ except